using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectUpMove : MonoBehaviour {
   // private object pos;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        Vector3 Vector3pos = transform.position;
     //   transform.position = new Vector3(pos., 2 * Mathf.Cos(3*Time.time),pos.z);
    }
}
